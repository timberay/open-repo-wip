<%= turbo_stream.update "repositories" do %>
  <% if @repositories.any? %>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      <% @repositories.each do |repository| %>
        <%= render "repository_card", repository: repository %>
      <% end %>
    </div>

    <% if @next_page %>
      <div class="mt-8 text-center">
        <%= link_to "Load More", 
          repositories_path(query: @query, page: @next_page, sort: @sort),
          class: "inline-block px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-colors",
          data: { turbo_frame: "repositories" }
        %>
      </div>
    <% end %>
  <% else %>
    <div class="text-center py-12">
      <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20 13V6a2 2 0 00-2-2H6a2 2 0 00-2 2v7m16 0v5a2 2 0 01-2 2H6a2 2 0 01-2-2v-5m16 0h-2.586a1 1 0 00-.707.293l-2.414 2.414a1 1 0 01-.707.293h-3.172a1 1 0 01-.707-.293l-2.414-2.414A1 1 0 006.586 13H4"/>
      </svg>
      <h3 class="mt-2 text-lg font-medium text-gray-900 dark:text-white">No repositories found</h3>
      <p class="mt-1 text-gray-500 dark:text-gray-400">Try adjusting your search terms</p>
    </div>
  <% end %>
<% end %>
